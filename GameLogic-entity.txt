Primero se refactorizo todo la logica para el modelo de base de datos:

* Migración de mapeo de entidades de Hibernate XML a anotaciones JPA

    - Dado que estaba obteniendo el error
        Schema-validation: wrong column type encountered in column [aliases] in table [Predicado];
        found [alias_list (Types#DISTINCT)], but expecting [testuser.alias_list (Types#VARCHAR)]

    para la columna aliases de las tablas de Teorema y Predicado tuve que ejecutar

        1) ALTER TABLE <table> ALTER COLUMN aliases TYPE TEXT;
        2) ALTER TABLE Teorema ALTER COLUMN aliases TYPE VARCHAR(255);

* Actualizar dependencias: hibernate, spring framework, etc...
-------------------------------------------------------------------


Luego, se creo el modelo de base de datos para la logica de juegos:


Modelo Nivel
------------

- Registra informacion relacionada a un nivel

* Identificador ID
* Numero del nivel
* Un nivel tiene muchos teoremas, un teorema solo puede pertenecer a un nivel

Modelo UserLevelProgress
------------------------

- Registra el proceso de un usuario en un nivel

* Cada userLevelProgress se asocia a un unico usuario, un usuario puede estar asociados a multiples userLevelProgress.
* Cada userLevelProgress se asocia a un unico nivel, un nivel puede estar asociados a multiples userLevelProgress.
* Un userLevelProgress se asocia a multiples resuelves, un resuelve se asocia a un unico userLevelProgress
* Numero total de puntos del usuario


El usuario Juan ha resuelto el teorema N del nivel X

Para agregar la columna points a la tabla teorema hice:
- ALTER TABLE Teorema ADD COLUMN points INTEGER NOT NULL DEFAULT 0;
-------------------------------------------------------------------

